# Copy to .env. Provide the key your configured provider needs.
ANTHROPIC_API_KEY=
# For OpenAI-compatible endpoints (vLLM/Ollama often need no real key):
# OPENAI_API_KEY=
# For the Telegram frontend (`work-agent telegram`):
# TELEGRAM_BOT_TOKEN=
